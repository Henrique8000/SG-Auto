package com.sgauto.app.controller;

import javafx.fxml.FXML;

/**
 * CONTROLLER da tela principal.
 * Recebe os eventos da View (principal.fxml) e aciona o model/service.
 */
public class PrincipalController {

    @FXML
    private void initialize() {
        // Chamado automaticamente após o FXML ser carregado.
    }
}
